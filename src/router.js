// import Vue from 'vue';
// import VueRouter from 'vue-router';
// import TaskManagementApp from './components/TaskManagementApp.vue'; // Update the path accordingly

// Vue.use(VueRouter);

// const routes = [
//   { path: '/', component: TaskManagementApp },
//   { path: '/complete', component: TaskManagementApp, props: { filter: 'complete' } },
//   { path: '/incomplete', component: TaskManagementApp, props: { filter: 'incomplete' } }
// ];

// const router = new VueRouter({
//   routes
// });

// export default router;
